[project]
name = "cloud-run-orchestrator"
version = "2.0.0"
description = "Cloud Run FastAPI Orchestrator & Managed Agent for Sandbox Runtimes"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "httpx>=0.27.0",
    "pydantic>=2.8.0",
    "google-genai>=0.1.1",
]
